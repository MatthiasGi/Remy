= render layout: 'list' do
  - if @recipe.image.attached?
    = image_tag @recipe.image, class: 'img-thumbnail d-block mx-auto my-2 mw-75 h-auto'
  %h1= @recipe.title
  %p= @recipe.description
  %p
    %i.far.fa-clock
    = duration @recipe
  = link_to recipe_cook_url(@recipe), class: 'btn btn-primary btn-lg btn-block' do
    = t '.cook'
